[metadata]
name = contract_intelligence_api
version = 0.1.0
description = Contract Intelligence API (local package for tests)
author = You
license = MIT

[options]
packages = find:
include_package_data = True
python_requires = >=3.8

[options.packages.find]
where = .
